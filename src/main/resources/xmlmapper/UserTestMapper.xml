<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.graduate.project.mapper.UserInfoMapper" >
    <!-- 通过管理员id查询管理员信息 -->
   <!-- <select id="findAdminInfoById" parameterType="int" resultType="com.graduate.project.entity.UserTest">
        select * from user where adminId=#{adminId}
    </select>-->

    <select id="findUserById" resultType="com.graduate.project.entity.User" parameterType="int">
        SELECT * FROM USER WHERE id=#{id}
    </select>

</mapper>
